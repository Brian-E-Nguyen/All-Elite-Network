const suffix = '| All Elite Network';

const HomePageTitle = `All Elite Network`;

const PPVPageTitle = `Pay Per View ${suffix}`;

const DynamitePageTitle = `Dynamite ${suffix}`;

const RampagePageTitle = `Rampage ${suffix}`;

const DarkPageTitle = `Dark ${suffix}`;

const DarkElevation = `Dark Elevation ${suffix}`;

export {
  HomePageTitle,
  PPVPageTitle,
  DynamitePageTitle,
  RampagePageTitle,
  DarkPageTitle,
  DarkElevation,
};
